import {
  formateTime,
  formateDate,
  calcTimes,
  calcSeconds
} from './date/index'

import other from './other/index'
const to = other.to
const omit = other.omit
const arrify = other.arrify

// 还需要增加浏览器类型判断

export {
  formateTime,
  formateDate,
  calcTimes,
  calcSeconds,
  to,
  omit,
  arrify
}